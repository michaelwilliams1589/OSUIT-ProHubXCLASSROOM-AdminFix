# Pro Hub X Classroom 10.3 Professional

Version 10.3 adds a visible running-version badge, atomic instructor approval, and staged account deletion that never reads or writes `/courseMembers`. The account is blocked first, then each cleanup step runs independently so one denied optional path cannot cancel the whole removal.
